            To-Do list

1. ComboBox setInsertMode

    QComboBox.NoInsert	Performs no insert.
    QComboBox.InsertAtTop	Inserts as first item.
    QComboBox.InsertAtCurrent	Replaces the currently selected item.
    QComboBox.InsertAtBottom	Inserts after the last item.
    QComboBox.InsertAfterCurrent	Inserts after the current item.
    QComboBox.InsertBeforeCurrent	Inserts before the current item.
    QComboBox.InsertAlphabetically	Inserts in alphabetical order.

    widget.setInsertPolicy(QComboBox.InsertAlphabetically)

2. Create QDoubleSpinBox; derived from QSpinBox         ********* DONE *********

3. QDial                                                ********* DONE *********

4. Implement SplitWindow

5. File Open and Save Dialog

6. Dir watcher

*7. Dialogs
    - Should return as such: res, text = app.questionPopup(...)

        Projects 

    1. DB Browser
    2. Notepad

    

2. Determine whether the app is being run in "iDE" or build mode (after freeze)

Such emptiness!

            fix

- app.getFont()
FontComboBox()
app.openFile()

    Features

1. 

    Important

use "py -m pip install -e ." for "hot reload"